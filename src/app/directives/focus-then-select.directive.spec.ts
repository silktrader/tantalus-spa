import { FocusThenSelectDirective } from './focus-then-select.directive';

describe('FocusThenSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusThenSelectDirective();
    expect(directive).toBeTruthy();
  });
});
